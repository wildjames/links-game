export const PATHS = {
    GAME: "/game",
    CREATE: "/",
}

export const BACKEND_URL = "http://localhost:3001/api"
export const BACKEND_PATHS = {
    CREATE: `${BACKEND_URL}/create`,
    FETCH: `${BACKEND_URL}/fetch/:gameId`,
}

export const ENV_CONFIG = {}
